{
	"name": "obsidian",
	"version": "1.0.0",
	"description": "A convenient wrapper for all the JS observers.",
	"main": "main.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "esbuild src/observers.ts --bundle --sourcemap --watch --outfile=main.js --servedir=/"
	},
	"keywords": [],
	"author": "Daniel B. Kazmer",
	"license": "GPL-3.0-only",
	"type": "module",
	"devDependencies": {
		"@biomejs/biome": "^2.3.8",
		"esbuild": "^0.27.1",
		"typescript": "^5.9.3"
	}
}
